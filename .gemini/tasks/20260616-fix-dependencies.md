# Task: Fix Dependencies & Package Configuration

## Checklist

- [x] Move `pino` from `devDependencies` to `dependencies` in `package.json`.
- [x] Remove `--webpack` flag from build script in `package.json`.
- [x] Run typescript checks and local production build test to verify configuration changes.
