# 📜 프로젝트 비즈니스 헌법 (GEMINI.md)

본 문서는 **SR(Service Request) 관리 시스템** 프로젝트의 최상위 비즈니스 헌법이다. 에이전트(Antigravity)와 개발자는 본 시스템의 모든 기능을 변경, 추가 또는 디버깅할 때 이 헌법에 정의된 비즈니스 정책을 최우선으로 준수해야 하며, 기술적 코딩 표준은 `.gemini/rules/` 하위의 기술 KI 문서를 따른다.

---

## 1. 사용자 역할 및 권한 규칙 (Role-Based Access Control)

시스템은 철저한 역할 기반 권한 제어(RBAC) 및 고객사 데이터 격리 원칙을 준수한다.

### 1.1. 사용자 역할 정의

- **시스템 관리자 (ADMIN)**: 전체 시스템 및 모든 고객사의 데이터를 조회, 생성, 수정, 삭제(CRUD)할 수 있으며, 시스템 설정과 전체 감사 로그 조회가 가능하다.
  <sub>⚠️ 남은 항목(2026-09-18): 시스템 설정 화면은 적용 중인 값(세션·비밀번호 정책·메일 서버·백업 일정)을 보여 주는 **읽기 전용**이다 — 설정을 저장할 곳이 없어, 저장하지 않으면서 성공을 돌려주던 저장 기능을 걷어냈다.</sub>
  <sub>✅ 준수(2026-09-18, 소유자 결정 D11): 전체 감사 로그는 ADMIN 전용 화면 '운영 > 감사 로그'(`/settings/audit`, `GET /api/audit-logs` — `policies.ensureCanViewAuditLogs`)에서 조회한다.</sub>
- **운영 관리자 (MANAGER)**: SR 접수(Intake) 및 담당 엔지니어 배정, 우선순위 조정을 담당하며, 전체 고객사의 데이터를 조회하고 처리할 수 있다. 단, **시스템 설정 변경, 사용자에 대한 역할 부여, 사용자·고객사·역할의 영구 삭제 권한은 갖지 못한다**(역할 부여는 ADMIN 전용 — 2026-09-18 소유자 결정 D5). SR은 전체 고객사 범위에서 삭제할 수 있다. SR 삭제는 논리 삭제(soft delete)라 삭제된 SR은 SR 목록·검색·상세·대시보드와 고객사 화면에서 사라지지만, 해당 SR의 댓글·첨부·이력은 지워지지 않고 보존된다(`.gemini/rules/db-rules.md` §2).
- **고객사 관리자 (CLIENT_ADMIN)**: 자신이 소속된 고객사의 사용자와 SR을 관리한다. 사용자 관리는 **조회·가입 승인/거절·비활성화(재활성화)** 이며 '자사 관리 > 자사 사용자' 화면에서 한다 — 사용자 생성과 역할 부여는 운영팀 업무다(2026-09-18 소유자 결정 D6). SR에 대해서는 **자사(소속 고객사) SR에 한하여 삭제(Delete)할 수 있다**. 타 고객사 SR 삭제는 정책 함수 `canDeleteSR`의 테넌트 조건에 의해 거부된다. SR 상태는 **확인완료·재오픈만** 바꿀 수 있고 거절·진행·보류·완료 같은 처리 전이는 하지 않는다 — 고객이 완료를 찍을 수 있으면 SLA 준수율이 벤더 성과 지표로서 의미를 잃는다(2026-09-18 소유자 결정 D2). 사용자·고객사·역할에 대한 삭제 권한은 갖지 못한다.
  <sub>근거: 2026-08-09 소유자 결정, 커밋 `6ac7f74`, 마이그레이션 `20260809120000_client_admin_sr_delete`. SR 삭제는 2026-08-15 부터 논리 삭제다(`srs.deleted_at`, 마이그레이션 `20260815100200_sr_soft_delete`). 삭제된 SR 은 앱에서 복구할 수 없고 그 첨부도 앱에서 열거나 지울 수 없지만, 댓글·첨부(서버 파일 포함)·활동·상태이력은 DB 와 스토리지에 그대로 남는다. 보존 기간과 물리 정리 배치는 아직 없으므로, 삭제 버튼은 개인정보 삭제 요청의 처리 수단이 되지 못한다.</sub>
- **담당 엔지니어 (ENGINEER)**: 자신에게 배정/할당된 SR에 한하여 조회, 처리, 완료 및 내부 노트/댓글을 작성할 수 있다. 타 엔지니어에게 할당된 SR은 임의로 변경할 수 없다. **SR 접수(Intake)와 담당자 배정·변경은 하지 않는다** — 운영 관리자 공용 큐 업무다(§4, 2026-09-18 소유자 결정 D1). 접수 업무에 필요한 **고객사 명부·서비스 카테고리 마스터 정보는 전체를 조회**할 수 있다(§1.2 참조). SR 삭제 권한은 갖지 못한다.
- **고객사 사용자 (CLIENT_USER)**: 자신이 속한(승인된) 고객사의 SR을 신청(생성)하고 **같은 고객사의 SR을 조회**할 수 있으며, 공개 댓글을 작성할 수 있다. 본인이 신청한 SR은 `내 요청` 화면에서 별도로 추적한다. 타 고객사의 SR·사용자·통계에는 접근할 수 없다.

> **확인완료(CONFIRMED)는 고객의 인수(Acceptance) 행위다.** 그 SR의 신청자 본인만 수행할 수 있으며 ADMIN·MANAGER·ENGINEER는 **남의 SR을** 대신 확인할 수 없다. 이는 권한(permission)이 아니라 신원(identity) 검사로 강제된다(§2 참조).
> 그래서 운영자가 고객의 요청(전화·메일 등)을 **대신 등록할 때는 실제 고객을 신청자로 지정한다**(대리 등록 — 2026-09-18 소유자 결정 D3). 대리 등록은 내부 사용자만 하며, 신청자는 그 고객사에 승인된 활성 고객 사용자여야 한다. 등록한 운영자는 활동·상태 이력의 행위자로 남는다.
> **예외 — 운영자가 자기 이름으로 등록한 SR**(2026-09-18 소유자 결정): 고객 신청자가 없으므로 **등록한 운영자 본인** 또는 **그 SR 고객사의 고객사 관리자(CLIENT_ADMIN)** 가 확인한다. 예전에는 이런 SR을 아무도 확인할 수 없어 '완료'에 영구히 머물렀다. 고객 사용자가 신청한 SR에는 이 예외가 없다 — 같은 고객사의 CLIENT_ADMIN이라도 남의 SR을 대신 확인하지 않는다. 판정은 `sr-state-machine.canConfirmAsAcceptor` 한 곳이다("운영자"는 신청자의 현재 역할이 ADMIN·MANAGER·ENGINEER 인지로 본다). ENGINEER 는 §1.1·§1.2 대로 배정된 SR 만 다루므로 자기가 등록한 SR 도 배정됐을 때만 확인하고, 그 밖에는 그 고객사의 CLIENT_ADMIN 이 확인한다. 이런 SR 의 완료 알림(§4 필수 알림)은 신청자인 등록 운영자에게만 가고 **CLIENT_ADMIN 에게는 보내지 않는다** — CLIENT_ADMIN 은 SR 목록·상세에서 '완료' 상태를 보고 확인한다(2026-09-18 소유자 결정).
>
> **접수 이후 SR 내용은 운영자만 고친다**(2026-09-18 소유자 결정 D4). 외부 사용자(신청자·고객사 관리자)는 접수 전(`REQUESTED`)에만 제목·본문 등 요청 내용을 고칠 수 있고, 그 뒤 변경은 댓글로 담당자에게 요청한다(만족도·추가 의견은 예외). SLA는 접수 시점의 요구를 근거로 산정되므로 그 뒤 요구 범위가 조용히 바뀌면 근거가 흔들린다. 완료 내용·거절 사유는 운영자만 쓴다. 판정은 `policies.ensureCanEditSRContent`(화면은 `sr-state-machine.canEditSRContentAt`) 한 곳이다.

### 1.2. 데이터 격리 및 감사 원칙

- **고객사 간 격리 (Multi-Tenant Isolation)**: 접근 범위는 세 단계로 구분한다.
  1. `ADMIN`·`MANAGER`: 전 고객사의 SR·사용자·통계에 접근할 수 있다.
  2. `ENGINEER`: **고객사 마스터 정보(고객사 명부, 서비스 카테고리)에 한하여 전체를 조회**할 수 있다. 접수·배정 업무에 필요한 최소 범위이며, SR 본문·고객사 사용자 정보·SR 통계는 자신에게 배정된 범위로 제한된다.
  3. 그 외 모든 사용자(`CLIENT_ADMIN`, `CLIENT_USER`): 자신이 명시적으로 소속되거나 할당받은 고객사를 벗어난 어떤 데이터에도 접근할 수 없다.

  **예외 — 가입 화면의 공개 고객사 목록**(2026-09-18 소유자 결정 D14 A+): 활성 고객사의 **이름·코드**는 공개해도 되는 기준 정보로 보고, 가입 화면이 로그인 없이 받는 목록(`GET /api/clients/public`)으로 누구에게나 준다. 로그인한 고객사 사용자가 이 목록으로 타 고객사의 이름·코드를 보는 것도 이 예외에 든다. 내부 id 는 주지 않고(가입은 코드로 서버가 고객사를 찾는다), IP 당 요청 수를 제한한다. 그 밖의 고객사 정보(연락처·계약·사용자·SR)는 위 3단계를 그대로 따른다.

  > ✅ **준수(2026-09-18) — ENGINEER 의 고객사 화면**: 고객사 상세·목록·조직도가 ENGINEER 에게 전 고객사의 사용자 명부(이름·이메일·역할)와 최근 SR·SR 건수를 보여 주던 것을 닫았다. 판정은 목록·상세와 같은 담당자 스코프(`policies.ts` 의 `clientSrScopeWhere`·`canViewClientRoster`)를 쓴다 — 사용자 명부는 싣지 않고(목록 행 펼침·사용자 시트 포함), SR 요약·건수는 배정분만 준다. 고객사별 **사용자 수(숫자)** 는 ENGINEER 에게도 보인다 — 소유자 확인(2026-09-18): 개인을 식별하지 않는 숫자라 "고객사 사용자 정보"에 들지 않으며 정상 기능이다. 같은 날 접수 API 로 남에게 배정된 SR 을 가로채던 경로도 닫았다(`ensureCanIntakeAssignedSR`, §1.1). 미배정 SR 의 접수는 운영 관리자(ADMIN·MANAGER 또는 `SR:INTAKE` 를 받은 커스텀 역할) 업무로 정했다(2026-09-18 소유자 결정 D1 — `policies.canIntakeSR`·`canAssignSR`, 시드 ENGINEER 의 `SR:INTAKE` 회수 마이그레이션 `20260918120000_role_permission_decisions`).

  API 및 Server Actions에서 이 데이터 격리가 누출되지 않도록 철저히 검증하며, **스코프 인자를 생략하면 전체가 반환되는 형태의 조회 함수를 만들지 않는다**. 스코프는 선택 인자가 아니라 필수 인자여야 하고, 누락은 런타임 전체 노출이 아니라 컴파일 실패로 드러나야 한다.

- **민감 데이터 보호**: 권한 변경, 삭제 등의 민감 행위는 반드시 Audit Log(감사 로그)로 영구 저장되어야 한다.
  구체 규칙(2026-09-18 소유자 결정 D11):
  - **범위**: 앱(API·Server Action)을 거친 행위는 `audit_logs` 에 같은 트랜잭션으로 남긴다. 시드·마이그레이션·부트스트랩으로 바뀐 권한은 저장소의 마이그레이션 파일(git 이력)을 감사 기록으로 본다.
  - **보존**: 영구다. 감사 로그를 지우는 경로를 만들지 않는다.
  - **행위자 보존**: 다른 사용자·데이터에 대한 관리 이력(가입 승인·비활성화·역할·고객사 관리 등)이 남은 계정은 영구 삭제하지 않고 비활성화로 둔다 — 영구 삭제하면 행위자 칸이 비워져(FK SET NULL) "누가 승인했나" 에 답할 수 없다. 본인에게 한 기록(비밀번호 변경·로그인)은 대상 칸에 신원이 남으므로 막지 않는다(`user.service.hardDeleteUser`). 운영 계정(ADMIN·MANAGER)은 삭제 경로로 다루지 않는다(`role-rules.isDeletionProtectedAccount` — 화면과 서버 공용).
  - **조회**: ADMIN 전용(§1.1).
- **인가 판정의 단일 지점**: 인가 판정의 정본은 `src/lib/policies.ts`의 `ensure*`·`can*` 함수다. API 라우트·Server Action·화면은 이 정책 함수를 **호출**할 뿐, 역할 문자열 비교를 자체적으로 복제하지 않는다. 화면과 서버가 서로 다른 규칙으로 판정하면 죽은 버튼이나 우회 경로가 생긴다.

### 1.3. 역할 상호 배타성 및 할당 제약 조건 (Role Exclusivity)

- **시스템 역할과 고객사 역할의 배타성**: 시스템 운영을 담당하는 역할(ADMIN, MANAGER, ENGINEER)과 특정 고객사에 종속된 역할(CLIENT_ADMIN, CLIENT_USER)은 한 사용자에게 동시에 부여될 수 없다.
- **고객사 할당 제약**: 특정 고객사(Client)에 소속된 사용자는 시스템 역할(ADMIN, MANAGER, ENGINEER)을 부여받을 수 없으며, 고객사에 소속되지 않은(미할당) 사용자는 고객사 역할(CLIENT_ADMIN, CLIENT_USER)을 부여받을 수 없다.

### 1.4. 기본 역할의 보호와 권한 조정 (2026-09-18 소유자 결정 RB-12)

- **기본 역할 5개(ADMIN·MANAGER·ENGINEER·CLIENT_ADMIN·CLIENT_USER)는 이름이 곧 정체성이다.** 세션에는 역할 이름만 실리고 인가·메뉴·알림·가입이 그 이름으로 판정한다. 그래서 개명은 표시 변경이 아니라 그 역할 사용자 전원의 인가 해제이고, 비워진 이름을 다른 역할이 차지하면 그 사용자가 내부 사용자로 판정돼 §1.2 격리가 풀린다. 따라서 **누구도(ADMIN 포함) 기본 역할의 이름을 바꾸거나 역할을 삭제할 수 없고, 다른 역할이 그 이름(대소문자 무시)을 가질 수 없다.** 설명은 고칠 수 있다. ADMIN 역할은 설명·권한까지 수정할 수 없다. 목록과 판정은 `src/lib/role-rules.ts`(화면과 서버 공용)와 `policies.ensureRoleNameAllowed`·`canDeleteRole` 이다.
- **§1.1 의 역할별 권한 구성은 기본값이다.** 시드(`prisma/seed.ts`)가 새 설치에 넣는 권한 매핑이며, ADMIN 은 운영 중 `/roles` 에서 조정할 수 있다 — 예: ENGINEER 에 `SR:DELETE`·`SR:INTAKE`, CLIENT_ADMIN 에 `SR:STATUS_CHANGE`·`USER:CREATE`, MANAGER 에 `ROLE:ASSIGN`·`CLIENT:DELETE`. §1.1 의 "갖지 못한다·하지 않는다"(소유자 결정 D1·D2·D5·D6 포함)는 이 기본값을 가리킨다. 조정은 감사 로그(`ROLE_PERMISSIONS_UPDATE`)에 남고, 부팅 시드는 이미 권한이 있는 역할을 되돌리지 않는다.
- **권한 조정으로 풀리지 않는 규칙**: 다음은 권한이 아니라 신원·범위로 코드에 고정돼 있어 어떤 권한을 줘도 바뀌지 않는다.
  - 확인완료의 주체(§1.1 인수 규칙 — 신청자 본인, 운영자가 등록한 SR 은 등록자 또는 그 고객사 CLIENT_ADMIN)
  - ENGINEER 배정 격리(§1.2 — 권한을 받아도 배정된 SR 만 조회·수정·삭제한다)
  - 고객사 간 격리(§1.2)와 역할 배타성(§1.3)
  - ADMIN 전용 기능(시스템 설정, 알림 아웃박스, 사용자 완전 삭제, ADMIN 역할 부여)과 ADMIN 역할 불변
  - 외부 사용자는 내부 역할을 부여할 수 없고, 역할에 권한을 붙일 때 행위자는 자기가 갖지 않은 권한을 줄 수 없으며 자기 역할은 고칠 수 없다(ADMIN 제외)

---

## 2. SR 라이프사이클 및 상태 전이 규칙 (SR State Machine)

모든 Service Request는 정해진 라이프사이클 상태 기계를 따르며, 조건 없는 상태 도약은 불가능하다.

```
[신청 (REQUESTED)] ──> [접수 (INTAKE)] ──> [진행중 (IN_PROGRESS)] ──> [완료 (COMPLETED)] ──> [확인완료 (CONFIRMED)]
        │                    │                      ↕                       │                        │
        └─── 거절 (REJECTED) ┘              [보류 (ON_HOLD)]                └──── 재오픈 ────────────┘
                  ↑                                 │                          (→ 진행중, 7일 이내)
                  └─────────────────────────────────┘
```

**전이표 (정본 — `src/lib/sr-state-machine.ts`의 `VALID_TRANSITIONS`와 1:1 대응한다)**

| 현재 상태     | 전이 가능 상태                                   |
| ------------- | ------------------------------------------------ |
| `REQUESTED`   | `INTAKE`, `REJECTED`                             |
| `INTAKE`      | `IN_PROGRESS`, `REJECTED`                        |
| `IN_PROGRESS` | `COMPLETED`, `ON_HOLD`                           |
| `ON_HOLD`     | `IN_PROGRESS`(보류 해제), `REJECTED`             |
| `COMPLETED`   | `CONFIRMED`, `IN_PROGRESS`(재오픈)               |
| `CONFIRMED`   | `IN_PROGRESS`(재오픈)                            |
| `REJECTED`    | 전이 없음(종단 상태). 재신청은 새 SR을 생성한다. |

> 다이어그램은 읽기 편의를 위한 것이고 **판정 기준은 위 전이표다.** 표에 없는 전이는 허용되지 않는다.

- **상태 변경 필수 조건** (코드가 강제하는 다섯 가지):
  - `신청`, `접수` 또는 `보류` 단계에서 `거절 (REJECTED)` 상태로 변경할 때는 **거절 사유(Rejection Reason)**가 반드시 폼 및 DB 데이터로 함께 기입되어야 한다.
  - `접수`에서 `진행중 (IN_PROGRESS)` 상태로 변경하려면 **담당자(assigneeId)가 배정**되어 있어야 한다.
  - `진행중`에서 `완료 (COMPLETED)` 상태로 변경할 때는 **완료 내용(Resolution Description)**이 누락 없이 작성되어야 한다.
  - `확인완료 (CONFIRMED)`는 그 SR의 **신청자 본인만** 수행할 수 있다. 운영자(ADMIN/MANAGER/ENGINEER)는 남의 SR을 대신 확인할 수 없다 — 확인은 고객의 인수(Acceptance) 행위다. 단, 운영자가 자기 이름으로 등록한 SR은 등록한 운영자 본인 또는 그 고객사의 CLIENT_ADMIN이 확인한다(§1.1 예외). 신원을 확인할 수 없으면 거부한다(fail-closed).
  - 보류 사유·재오픈 사유는 `sr_status_history.change_reason`에 영속된다.
- **보류 (ON_HOLD)**: 진행 중 외부 요인(고객사 응답 대기 등)으로 인해 보류 상태로 전이할 경우, **보류 사유**와 **예상 해제일**을 명시한다.
  > ✅ **준수(2026-08-15)**: `srs.expected_hold_release_date` 컬럼과 다이얼로그 입력란을 추가했다. 보류 해제(resume) 시 null 로 되돌려 지난 약속이 다음 보류의 필수 검사를 통과하지 못하게 한다.
- **재오픈 (Reopen)**: 재오픈 가능 창은 **7일**이며, 기산점은 출발 상태에 따른다 — `완료(COMPLETED)`에서 재오픈하면 `completed_at`, `확인완료(CONFIRMED)`에서 재오픈하면 `confirmed_at`을 기산점으로 한다. **재오픈 사유**를 필수로 기록하고 상태를 `진행중(IN_PROGRESS)`으로 전이시키며, 이력(`SRActivityType.REOPENED`)을 남긴다. 재오픈은 별도의 상태 Enum을 두지 않는다.
  - **fail-closed 원칙**: 기산점이 되는 시각을 확인할 수 없는 SR(레거시 데이터 포함)은 재오픈을 **거부**한다. 시각이 없다는 이유로 창 검사를 건너뛰어 무기한 재오픈을 허용해서는 안 된다.
    단, `확인완료`에서 출발했는데 `confirmed_at` 만 비어 있으면 `completed_at` 을 기산점으로 쓴다 — 확인은 완료 뒤에만 일어나므로(`completed_at ≤ confirmed_at`) 창이 넓어지지 않고 오히려 좁아진다. 두 시각이 모두 없으면 거부한다(`sr-state-machine.ts` 의 `reopenAnchor`).
  - **재오픈과 SLA**: 재오픈된 SR은 **최초 마감일(`due_date`)을 그대로 유지하며 SLA 판정 대상으로 남는다**(재작업은 서비스 실패로 계상한다). 단 재오픈 사유가 고객의 요구 범위 변경인 경우에 한해 접수 담당자가 마감일을 재산출할 수 있으며, 이때 사유를 기록한다.
    구체 규칙(2026-09-18 소유자 결정 D9 — 판정은 `sr-state-machine.canViewerAdjustDueDate`·`canChangeSLABasisAt`·`isRejectBlockedAfterCompletion`, 서버는 `sr.service.updateSR`):
    - 한 번이라도 완료된 SR(재오픈 포함)은 카테고리·실제 우선순위가 바뀌어도 **마감일을 자동 재산출하지 않는다** — 이 절이 §3 의 재산출 트리거보다 우선한다.
    - 그런 SR 의 마감일은 §3 수동 조정으로만 바꾸며, 조정은 **접수 권한자(ADMIN·MANAGER 또는 `SR:INTAKE`)만** 한다. 재작업 당사자가 기준 마감일을 옮기지 못하게 한다. '요구 범위 변경' 여부는 조정 사유로 남긴다.
    - 종결(완료·확인완료·거절)된 SR 의 마감일·서비스 카테고리·실제 우선순위는 바꾸지 않는다. 고쳐야 하면 재오픈한 뒤 고친다.
    - 한 번 완료된 SR 은 **거절로 끝내지 않는다** — 재작업을 마치면 다시 완료로 종결한다(재오픈 → 보류 → 거절로 준수율 표본에서 빠지는 경로를 막는다).
    - 마감일 조정은 SR 활동에 "SLA 마감일 조정: 이전 → 이후" 로 남고, 조정 사유는 내부 사용자에게만 보인다(`policies.redactActivityForViewer`).
  - **재오픈 횟수**: 제한하지 않는다(2026-08-15 확인). 과거 산출물의 "1회만" 서술은 폐기한다.
  > ✅ **준수(2026-08-15)**: 기산점을 출발 상태별로 분기하고(CONFIRMED→`confirmed_at`), 기산점을 알 수 없으면 거부하도록 fail-closed 로 뒤집었으며, 레거시 행은 마이그레이션으로 백필했다. `REOPENED` 활동 이력도 기록한다. 창 판정 사본을 들고 있던 라우트 코드는 제거해 `validateTransition` 한 곳으로 모았다.

---

## 3. SLA 및 마감일 관리 정책 (SLA & Deadline Management)

시스템은 우선순위별 고정 시한 대신, 고객사의 서비스 카테고리별로 설정된 기본 SLA 목표 시간(slaHours)과 우선순위 배율을 곱하여 마감일을 지정 및 감시하는 동적 SLA 관리 정책을 수행한다.

- **마감일 동적 산출**: SR 접수(Intake) 시점에 실제 우선순위(Critical, High, Medium, Low)가 확정되면, 해당 서비스 카테고리의 기본 SLA 시간(slaHours)에 **우선순위별 가중치 배율**을 곱하여 최종 처리 마감일(`due_date`)을 동적으로 산출한다.
  - **긴급 (Critical)**: 기본 SLA 시간의 50% (0.5 배율)
  - **높음 (High)**: 기본 SLA 시간의 75% (0.75 배율)
  - **중간 (Medium)**: 기본 SLA 시간의 100% (1.0 배율)
  - **낮음 (Low)**: 기본 SLA 시간의 150% (1.5 배율)
- **마감일 재산출 트리거**: 실제 우선순위(`actualPriority`) **또는 서비스 카테고리**가 변경되면 마감일을 재산출한다. 재산출은 항상 **변경 후**의 카테고리 `slaHours`를 근거로 한다. 변경 전 값으로 계산하면 화면에 표시된 SLA 근거와 저장된 마감일이 어긋난다.
- **마감일 수동 조정**: 마감일은 원칙적으로 자동 산출값을 사용한다. 운영자(`ADMIN`·`MANAGER`·배정된 `ENGINEER` 또는 `SR:ASSIGN` 보유자)에 한해 수동 지정할 수 있으며(한 번 완료된 SR 은 접수 권한자만 — §2 재오픈과 SLA), 이때 **조정 사유를 기록하고 감사 로그를 남긴다**. 수동 조정된 마감일은 이후의 우선순위·카테고리 변경에 의한 자동 재산출로 **덮어써지지 않는다**. 마감일을 비우는(삭제) 조작은 허용하지 않는다.
- **SLA 준수율 모니터링**: 최종 처리 완료 시각이 산출된 마감일(`due_date`) 이내인지를 감시하여 SLA 준수 여부를 판정하며, 이는 대시보드 통계 및 준수율(`slaComplianceRate`)에 실시간 반영된다.
- **SLA 준수율의 모수 정의**: 준수율은 「집계 창 내에 **완료된** SR 중 마감일이 산출된 건」을 모수로, 「`completed_at ≤ due_date`인 건」을 분자로 계산한다. 집계 창은 **완료 시각 기준 최근 30일**로 한다.
  - 마감일이 산출되지 않은 SR은 분모·분자 **양쪽 모두에서 제외**하고, 그 건수를 "측정 불가" 지표로 별도 노출한다. 분모에만 넣으면 측정하지 못한 건이 영구 위반으로 집계된다.
  - 아직 완료되지 않았으나 마감일을 넘긴 SR은 준수율이 아니라 별도의 "지연 중" 지표로 표시한다.
  - **표본 없음(0건)과 준수율 0%는 화면에서 구분되어야 한다.** 둘을 같은 기호로 표시하면 SLA 전면 붕괴가 "데이터 없음"으로 읽힌다.
- **마감 초과 판정은 시각 비교로 한다**: 달력일(날짜) 단위로 판정하면 12시간 SLA를 넘긴 SR이 하루 종일 "오늘 마감"으로 보인다. 24시간 미만 잔여는 시각까지 표기한다.
- **SLA 초과 능동 경고**: 주기적 SLA 임박/초과 스캔 및 반복 경고 알림은 **본 시스템의 규범에 포함하지 않는다**(미도입). 시간 기반 스케줄러가 없으며, 감시는 대시보드 통계·목록 표시로 수행한다.

> ✅ **준수(2026-09-18, 소유자 결정 D10) — '지연 중' 지표**: 진행 중 상태(접수·진행중·보류 — `sr-state-machine.SR_SLA_OPEN_STATUSES`)이면서 마감을 넘긴 SR 을 센다. 보류도 넣는다 — 보류 중에도 SLA 시계가 멈추지 않아 결국 위반으로 집계되므로, 선행·후행 지표가 같은 기준을 쓴다(행 표시는 '보류 · N일 지연'). 대시보드(내부 사용자 전용)에 '지연 중 N건(보류 k건 포함)' 카드와 '마감일 직접 조정 m건'을 두고, 카드를 누르면 같은 범위의 목록(`/srs?overdue=1`, 빠른 필터 '지연')이 뜬다. '오늘 마감' 배지는 지금 이후 오늘 자정 전만 센다(이미 지난 건은 '지연'). 준수율 카드는 표본 0건을 '표본 없음', 마감일이 없는 건을 '측정 불가'로 부른다. 고객에게는 합계를 보이지 않는다(행별 'N일 지연' 표시는 원래 보인다). 한계: SLA 시계는 접수 시각부터 돌므로 **접수되지 않고 쌓인 요청은 이 지표에 잡히지 않는다** — '평균 대기 시간' 카드가 이를 보인다.

> ✅ **준수(2026-08-15)**: 재산출 트리거를 카테고리 변경까지 넓히고 **변경 후** 카테고리로 계산하도록 고쳤다. 수동 지정된 마감일은 자동 재산출이 덮어쓰지 않는다. 준수율은 `due_date IS NULL` 을 분모·분자 양쪽에서 제외하고 집계 창을 `completed_at` 기준으로 바꿨으며, 표본 수와 측정 불가 건수를 함께 내려보내 화면이 "표본 없음"과 "0%"를 구분한다.
>
> ⚠️ **정정(2026-09-18)**: 아래 2차 준수 표기 가운데 **수동 조정 규칙은 실제로는 지켜지지 않고 있었다.** 수동 여부를 저장하지 않아 다음 우선순위·카테고리 변경 때 덮어써졌고, 마감일을 비울 수 있었으며(비우면 준수율의 분모·분자에서 빠진다), 조정 사유는 필수가 아니었다. 같은 날 헌법대로 구현했다(소유자 결정 D8): `srs.due_date_manual` 표식(마이그레이션 `20260918121000_sr_due_date_manual`, 기존 조정은 감사 로그로 백필), 사유 필수·비우기 금지(`sr.service.updateSR`), 접수·접수 정보 수정 경로도 표식을 존중, SR 상세의 'SLA 마감일' 칸(예전에는 예상 완료일을 보여 줬다)과 '마감일 조정' 다이얼로그.
>
> ✅ **준수(2026-08-15, 2차)**: 마감 초과 판정을 시각 비교로 바꿨고(24시간 미만 잔여는 시각 표기, 24시간 미만 지연은 시간 단위 표기), 마감일 수동 조정은 `SR_DUE_DATE` 감사 로그를 같은 트랜잭션에 남긴다. 서비스 카테고리도 SLA 산정 근거이므로 운영자 소유 필드로 게이트했다. "오늘 마감" 범위 질의의 앰비언트 타임존 사용처도 앱 타임존 경계(`startOfAppZoneDay`)로 교체했다.

---

## 4. 자동 알림 및 연동 정책 (Notification System)

모든 주요 상태 변화는 비동기식 이벤트 알림을 유발해야 한다.

- **알림 채널**: 사용자 설정에 따라 **이메일**과 **웹 푸시(Web Push)** 로 발송된다.
  - 인앱 알림(알림 벨·인박스)은 **구현하지 않는다**(소유자 결정 2026-08-02). 예전 이 문서는
    인앱 알림을 채널로 명시했지만 실체가 없었다 — 알림 벨도, `/api/notifications` 라우트도,
    `realtime-events.ts:26` 의 `NOTIFICATION_RECEIVED` 를 발행하거나 수신하는 코드도 없다.
    없는 기능을 있다고 적어 둔 문서는 이 시스템을 처음 읽는 사람을 잘못된 곳으로 보낸다.
  - SSE(`GET /api/realtime`)는 접속 중인 화면의 **데이터 갱신용**이며 알림 전달 채널이 아니다.
  - 이 결정에 따라 `NotificationType.IN_APP` enum 값과 `REALTIME_EVENTS.NOTIFICATION_RECEIVED` 상수는 **제거 대상 잔재**로 분류한다. 신규 코드는 이 두 심볼을 참조하지 않으며, 알림 아웃박스는 `EMAIL`(및 향후 `PUSH`)만 적재한다. 스키마에서 `IN_APP`을 제거하는 마이그레이션은 별도 정리 커밋으로 수행한다.
- **신청 단계**: SR이 등록되면 **활성 상태의 운영 관리자(`ADMIN`·`MANAGER`) 전원**에게 즉시 알림이 발송되어야 한다. 접수(Intake)는 특정 담당자가 아니라 운영 관리자 공용 큐에서 처리하기 때문이다. `ClientHandler`(고객사 담당자)와 `ServiceCategory.handlerId`는 조직 정보 관리용이며 알림 수신자 산정에 사용하지 않는다.
- **배정 단계**: SR 담당자가 지정 또는 변경되면 신규 배정된 담당자에게 알림이 전송되어야 한다.
- **재오픈 단계**(2026-09-18 소유자 결정 D15): SR 이 재오픈되면 다시 일해야 하는 **담당자에게** 재오픈 사유를 담아 알린다. 켜고 끄는 설정은 배정 알림(`emailSRAssigned`·`pushSRAssigned`, 기본 켜짐)을 따른다 — 재오픈은 일이 다시 맡겨진 것이다. 담당자가 비활성 계정이면 재배정할 활성 ADMIN·MANAGER 에게 알린다. 예전에는 상태 변경 알림을 신청자만 받아, 고객이 재오픈해도 담당자는 접속 중일 때의 토스트 말고는 알 길이 없었다(구현: `sr-email-outbox.enqueueSRReopenedEmails`, `sr-notification.listener`).
- **행위자 제외**(D15): 모든 상태 변경 알림은 그 전이를 일으킨 사람 본인에게 보내지 않고, 같은 사람에게 두 통을 보내지 않는다. 푸시 본문은 화면과 같은 한국어 상태 이름을 쓴다.
- **댓글과 내부 노트**: 댓글이 달리면 신청자와 담당자(작성자 본인 제외)에게 사용자 설정에 따라 알린다. **내부 노트**(내부 사용자만 쓰고 읽는, 고객에게 보이지 않는 댓글)는 신청자에게 알리지 않으며, 고객도 보는 활동 로그에 남기지 않고, 실시간 이벤트도 내부 사용자에게만 흘린다 — 노트의 존재 자체가 고객에게 드러나지 않아야 한다(2026-09-18 소유자 결정 D7, `policies.canWriteInternalNote`·`canReceiveRealtimeEvent`).
- **완료 및 거절 단계**: SR 신청자가 결과를 확인할 수 있도록 즉시 완료/거절 알림을 수신자 이메일로 전송한다. **이 두 알림은 사용자 설정과 무관한 필수 알림이다** — 신청자가 처리 결과를 통보받지 못하는 상태를 시스템이 기본값으로 두어서는 안 된다. 메일 본문에는 거절 사유 또는 완료 내용이 포함되어야 한다.
  > ✅ **준수(2026-08-15)**: `COMPLETED`·`REJECTED` 전이는 `emailSRStatusChanged` 게이트를 타지 않는 **필수 알림**으로 승격했다. 나머지 전이(접수·진행중·보류·확인완료)만 사용자 설정을 존중한다. 메일 본문에는 완료 내용 또는 거절 사유가 함께 실린다.

### 4.1. 전달 신뢰성 (2026-08-02)

- **이메일은 아웃박스로 전달한다**: 도메인 트랜잭션 **안에서** `notifications` 테이블에 `PENDING`으로 적재한다. 트랜잭션이 롤백되면 알림도 함께 사라지므로 "없던 일에 대한 알림"이 나가지 않는다. 발송은 별도 디스패처가 `FOR UPDATE SKIP LOCKED`로 claim해 수행하며, 다중 인스턴스에서도 중복 발송되지 않는다.
- **재시도와 dead-letter**: 실패는 지수 백오프로 재시도하고 상한 도달 시 `FAILED`로 고정하며 `failReason`에 마지막 예외를 남긴다. **재시도 상한과 백오프 간격의 정본은 `src/services/notification-outbox.ts`의 `MAX_ATTEMPTS`·`BACKOFF_MINUTES` 상수이며, 본 문서는 숫자를 복제하지 않는다.**
- **웹 푸시는 최선 노력(best-effort) 채널이다**: 재시도·dead-letter를 적용하지 않으며, 실패는 구독 단위로 격리하고 만료 응답(410/404)을 받은 구독만 정리한다. 푸시를 아웃박스로 편입하려면 `notifications`에 `type='PUSH'` 행을 적재하고 디스패처 claim 조건을 함께 넓힌다.
- **발송 실패는 조회 가능해야 한다**: 실패한 알림을 운영자가 확인하고 재발송할 수 있는 경로를 제공한다.
  > ✅ **준수(2026-08-15, 2차)**: `GET /api/notifications/outbox`(ADMIN 전용)로 상태별 조회와 실패 사유·시도 횟수를 확인할 수 있다. 본문(`content`)은 응답에서 제외한다 — 운영자가 알아야 할 것은 "무엇이 왜 실패했는가"이지 수신자에게 간 본문이 아니다. 보존 정리는 디스패처가 1시간에 한 번 수행하며 `SENT` 90일 / `FAILED` 1년 경과분을 지우고, 발송에 성공한 행은 본문을 즉시 비운다. 임대 시간은 배치 최악 소요(배치 크기 × SMTP 타임아웃)에서 도출하도록 바꾸고 tick 에 재진입 가드를 뒀다 — 예전 5분 고정값은 배치보다 짧아 같은 행이 중복 발송될 수 있었다.
  >
  > ✅ **준수(2026-08-15 구현, 2026-09-18 표기 정정)**: 조회 화면 `/settings/outbox`(사이드바 '운영 > 알림 발송 이력', ADMIN 전용)와 재발송(`POST /api/notifications/outbox`)이 있다. 재발송은 `FAILED` 행만 `PENDING` 으로 되돌리고 시도 횟수를 0 으로 리셋하며, 실제 발송은 다음 디스패처 tick 이 맡는다. 예전 이 자리의 "화면·재발송 없음" 표기는 구현보다 뒤처진 것이었다.
